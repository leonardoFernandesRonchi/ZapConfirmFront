services:
  frontend:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true  
    command: sh -c "npm install && npm run dev -- --host"
    ports:
      - '5173:5173'
